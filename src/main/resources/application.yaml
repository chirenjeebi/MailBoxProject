server:
  port: 9021


spring:
  datasource:
    url: jdbc:mariadb://database-1.crm2rxoxsody.us-east-1.rds.amazonaws.com:3306/mailboxdb
    username: mailboxuser
    password: mypassword
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    hibernate:
     ddl-auto: update
